# ADR-002 Use server_name impro.chat

Status: Accepted

`server_name` is the suffix of every Matrix ID and cannot be changed later. Physical CS/federation traffic is delegated to `matrix.impro.chat` via well-known. IDs remain `@user:impro.chat`.
