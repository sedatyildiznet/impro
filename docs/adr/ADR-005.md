# ADR-005 Keep business sharing in the Impro layer

Status: Accepted

Inviting workspace members into the owner's bridged Matrix room would leak history. Sharing is metadata + a filtered view + delegated send through the owner's session.
